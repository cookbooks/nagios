# Hostgroup definitions
# Automatically generated by Chef

define hostgroup {
  hostgroup_name servers
  alias servers
}

define hostgroup {
  hostgroup_name devices
  alias devices
}

<% @roles.each do |name, conf| %>
define hostgroup {
  hostgroup_name <%= name %>
  alias <%= conf[:alias] || name %>
}

<% end %>

